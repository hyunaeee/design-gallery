# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/

# Node (not used, but just in case)
node_modules/

# Temporary / raw AI asset intermediates (kept locally, not committed)
assets/gen/*-raw.png
assets/gen/*-raw.jpg
